- name: mysql
  hosts: web
  become: yes
  tasks: #install mysql
  - name: mysql-server
    ansible.builtin.package:
     name: mysql
     state: present

  - name: run mysql
    ansible.builtin.service:
     name: mysql-server
     state: started
     enabled: yes