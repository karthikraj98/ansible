- name: loops demo
  become: yes
  hosts: web
  tasks:
  - name: print the list of names
    ansible.builtin.package:
      name: "{{ item }}"
      state: present
    loop:
    - mysql
    - nginx
    - git
    - httpd

    name: "{{ item }}"
    state: present
    enabled: yes
    loop:
    - mysql
    - nginx
    - git
    - httpd